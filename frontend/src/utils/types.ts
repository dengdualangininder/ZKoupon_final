export type Rates = { usdcToLocal: number; usdToLocal: number };
export type FlashInfo = { userType: string; flashToken: string; isUsabilityTest: boolean };
export type UserInfo = { idToken: string; publicKey: string };
